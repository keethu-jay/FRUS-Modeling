# LiDAR point clouds — too large for git (use scp/rsync from Turing)
*.laz
*.las

# Large intermediate rasters generated by the Turing pipeline
# (keep final_mask.tif which is small enough and needed by the frontend)
dem.tif
dem_reprojected.tif
watershed_basins.tif
flood_outputs/

# XYZ tile trees — generated by gdal2tiles, can be GBs
frontend/public/data/permeability/
frontend/public/data/flood/

# Split shapefile chunks for Mapbox uploads (large); regenerate with scripts/split_shapefile_zip.py
frontend/public/data/topo_chunks/

# Python
__pycache__/
*.pyc
*.pyo
.env

# Turing job logs
turing/slurm/logs/

# Local Claude / IDE (machine-specific)
.claude/

# Oversized GIS inputs — regenerate locally or fetch separately (would blow repo limits)
/topo_vector.zip
/mask_vector.zip
/nyc_curbs.geojson
/NYCDEP*.geojson
/New_York_City_Climate*.csv
/final_mask*.tif
/flood_scenarios.zip
frontend/public/data/topo_vector.zip
frontend/public/data/mask_vector.zip
